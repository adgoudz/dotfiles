
pathmunge() {
    DIR=$1
    AFTER=$2

    # Remove existing
    PATH=${PATH/#${DIR}:}
    PATH=${PATH//:${DIR}}

    if [[ $AFTER = 'after' ]]; then
        export PATH=${PATH}:${DIR} # last
    else
        export PATH=${DIR}:${PATH} # last
    fi
}
