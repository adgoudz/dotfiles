
# Color output (auto set to 'no' on dumb terminals).
zstyle ':prezto:*:*' color yes

#
# Set Prezto modules to load
#

# 'editor' \
# 'history' \
# 'directory' \
# 'spectrum' \
# 'utility' \

zstyle ':prezto:load' pmodule \
  'git' \
  'prompt'

#
# Prompt
#

# Set the prompt theme to load.
zstyle ':prezto:module:prompt' theme 'andrew'

#
# Git
#

# Set a format for oneline log output
zstyle ':prezto:module:git:log:oneline' format '%C(bold magenta)%h%Creset %C(cyan)%<(12,trunc)%an%Creset %<(24)%C(bold yellow)%ad%Creset %<(70,trunc)%s %C(bold red)%d%Creset'

# vim: ft=zsh
